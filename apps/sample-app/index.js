/**
 * @format
 */

import { AppRegistry } from 'react-native';

import ContextProviders from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => ContextProviders);
