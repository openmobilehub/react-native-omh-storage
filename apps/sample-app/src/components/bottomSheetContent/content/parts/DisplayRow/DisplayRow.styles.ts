import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    columnGap: 8,
  },
  labelText: {
    marginTop: 2,
  },
  valueText: {
    flex: 1,
    flexWrap: 'wrap',
  },
});
