import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'column',
    padding: 16,
  },
  button: {},
  footer: {
    flexDirection: 'row',
    gap: 8,
    marginTop: 16,
  },
});
