import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'column',
    rowGap: 16,
  },
  header: {
    flexDirection: 'column',
    rowGap: 16,
    marginTop: 8,
    marginBottom: 8,
  },
  button: {
    alignSelf: 'baseline',
  },
});
