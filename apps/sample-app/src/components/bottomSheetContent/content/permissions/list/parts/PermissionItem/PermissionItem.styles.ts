import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'column',
    padding: 16,
  },
  image: {
    width: 64,
    height: 64,
  },
  footer: {
    flexDirection: 'row',
    gap: 8,
    marginTop: 16,
  },
});
