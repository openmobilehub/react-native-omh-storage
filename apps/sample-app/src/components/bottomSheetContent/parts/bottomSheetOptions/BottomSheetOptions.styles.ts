import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  menuItem: {
    maxWidth: 'auto',
  },
  fileName: {
    marginBottom: 16,
    paddingHorizontal: 16,
  },
  permanentDeleteItem: {
    color: 'red',
  },
});
