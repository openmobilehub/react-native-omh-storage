{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build:android": {
      "outputs": ["android/app/build/outputs/apk/release/**"]
    },
    "build:ios": {
      "outputs": ["ios/build/RNStorageSample.xcarchive/**"]
    }
  }
}
